# Worklog — 2026-07-21-store-stage-1-capture-boundary

## 2026-08-02 — session 1 recorded here (records-match-the-tree pass)

- Moved `1_in-progress/` → `3_in-review/`. The folder was lying: the code shipped
  in PR #50 (task header says so) and `docs/roadmap/current-state.md:47` lists the
  capture boundary among "stages 0–4 shipped", yet nothing had touched the task
  in 12 days. `1_in-progress` means "claimed and being worked"; it was neither.
- Nothing inside the task changed except this worklog and a new `verification.md`
  (required by `tasks/README.md` for `3_in-review`). No DoD box was ticked.
- What still blocks `4_done`: the last DoD bullet — several days of real search
  runs measured for growth/run, capture overhead and dedup ratio. That is owner
  work; an agent cannot produce it. Its four store/email siblings sit in
  `3_in-review/` for the same class of reason.
- Deliberately left alone: `2026-07-22-email-progress-reconciliation`, the other
  `1_in-progress/` task, which is genuinely in flight.
