# Handover — <session slug>

- **Date**: <YYYY-MM-DD>
- **Task(s)**: <task id(s), or "none">

<One screen max, plain language, written for a human who was away. Depth
goes in the task folder — link it, don't restate it. Prose rules:
`docs/handbook/reporting-to-the-owner.md`.>

## What happened

- <OPEN with the state of the world in one line: is anything in flight,
  half-done, or broken? The first thing the next reader needs is not what you
  did, it is whether anything is on fire.>
- <then the outcome, not the play-by-play>

## Where things stand

- <state of the work: merged / in review / blocked-on-X>

## Decisions made for you

- <one line each: what was chosen, why, and what undoing it would cost. Link
  the ADR or task that records it. Include the obviously-right ones — the
  reader cannot tell an obvious call from a load-bearing one until they see
  it. If one was expensive to reverse it should have been a
  `needs-human/decisions/` item instead: say so here. "None." is valid.>

## If X then Y

- <the contingencies you are carrying in your head that would change what the
  next person does: "if the nightly run is still red, the cause is X, not Y".
  This is the part that is always lost and never missed until it hurts.
  "None." is valid.>

## Dead ends

- <what was tried and abandoned, one line each: what was tried, what happened,
  what it means. Without this the next session spends its first hour
  rediscovering your worst hour. "None." is valid.>

## Needs your attention

- <one line per open needs-human/ item: a link, plus `Why this matters` and
  `If you do nothing` — never a bare filename. Repeat items still open from
  EARLIER sessions, not only today's; an item that stops being mentioned is an
  item that silently died. "Nothing" if empty.>
