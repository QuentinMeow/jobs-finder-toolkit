# macOS
.DS_Store

# Python
__pycache__/
*.pyc
# No trailing slash: `.venv/` matches only a DIRECTORY, and a git worktree is
# usually given its interpreter as a SYMLINK named .venv. That symlink is not
# ignored, shows as untracked, and its blob is an absolute path under the
# owner's home dir — which the leak guard cannot catch, because it scans file
# CONTENTS and a symlink has none to scan.
.venv
# Scratch
local/
# Local machine config (copy config.example.yaml -> config.yaml and edit)
config.yaml
# Local Claude Code settings (personal permissions + opt-in metrics hooks; see docs/handbook/metrics.md)
.claude/settings.local.json
# Agent worktrees. The harness creates one full checkout per parallel subagent
# here. They are scratch — the work reaches this repo through a branch or a
# commit, never through this directory — but they were NOT ignored, so a single
# `git add -A` staged four complete copies of the repo, one of which held an
# unreviewed tree. Anchored to the root: only the harness writes here.
/.claude/worktrees/
# Local metrics collection output (opt-in metrics hooks; see docs/handbook/metrics.md)
# Anchored to the repo root, like the product rules below. The only writer is
# automation/metrics/hook_collect.py, whose LOG_PATH is REPO_ROOT/"logs". Left
# unanchored this matched a `logs/` directory at ANY depth, which silently made
# examples/market/logs/** untrackable: `git mv` into it reports success, and the
# files are then invisible to `git ls-files`, to export_public.py, and to the
# leak guard — a tracked fixture that does not exist as far as any gate can tell.
/logs/
# Cross-session scratch memory (ephemeral; see AGENTS.md Memory Map)
.cursor/MEMORY.md
# Private overlay (personal data mounts here; never committed to the public repo).
private/
# Agent scratch memory (personal cross-session notes; see AGENTS.md "Memory Map")
.agents/MEMORY.md
# Runtime entries for overlay-only skills are created by
# automation/bootstrap_overlay.py. Their exact names are stored only in the
# checkout's repository-local .git/info/exclude file, never in this tracked file.
# NOTE (workspace-restructure phase 4, 2026-07-28): every rule that used to hide
# overlay content sitting at a path under skills/ is gone — the private-skill
# symlink and in-place rules, the personal search-profile glob with its two
# negations, and the per-skill private-notes pair. Nothing under skills/ is
# private any more; the overlay is reached through config accessors and the
# runtime entries above. A gitignore glob was never the guard anyway: `git add -f`
# overrode all of them. check_public.py is.
# Optional git-ignored personal token list consumed by the leak guard
private/leak_tokens.txt
private/leak_safe_words.txt
# Private products (present only if you work in-place with your own data).
# Anchored to the repo root so they never match the tracked examples/ dataset
# (e.g. examples/me/applications/**,
# examples/me/career/resume/reference.example.docx).
# NOTE: the former /templates/ in-place rule was retired 2026-07-22 — root
# templates/ is now the TRACKED process-file schema folder; in-place resume
# reference DOCX belongs under private/ (see docs/handbook/private-overlay.md).
/applications/
/interviews/
/.agents/inputs/
# Generated dated discovery reports under the EXAMPLE config (test/canary runs
# write real dated files here because config.example.yaml points discoveries_dir
# at this tracked dir). Only .gitkeep is meant to be tracked.
examples/market/scans/current/*.md
# Search profiles copied into the EXAMPLE config's search_profiles_dir. The
# directory is tracked (.gitkeep) so a bare `--profile <label>` resolves on a
# fresh clone, but a profile written there is the user's own search vocabulary —
# candidate data, never publishable. Same shape as the rule above.
examples/market/searches/*.yaml
